/**
 * IRecommendationStrategy Interface
 * Strategy pattern interface for mapping recommendation candidates.
 */
export class IRecommendationStrategy {
  /**
   * Evaluates user context to recommend next steps.
   * @param {object} context Contains: uid, graph, progressList, masteryList
   * @returns {Promise<Recommendation[]>} Array of recommendations generated by this strategy
   */
  async evaluate(context) {
    throw new Error('Method not implemented: evaluate');
  }
}
export default IRecommendationStrategy;
